"use client";
import Link from "next/link";
import { useParams } from "next/navigation";
import { useEffect, useState } from "react";
import { apiGet } from "@/lib/api";
import type { User } from "@/types/api";
export default function UserDetailPage() { const params = useParams<{ id: string }>(); const [user, setUser] = useState<User | null>(null); const [message, setMessage] = useState(""); useEffect(() => { apiGet<User>(`/admin/users/${params.id}`).then(setUser).catch((reason: Error) => setMessage(reason.message)); }, [params.id]); return <main className="mx-auto max-w-7xl px-4 py-8"><div className="mb-6 flex items-center justify-between"><h1 className="text-3xl font-bold text-slate-950">Chi tiet tai khoan</h1><div className="flex gap-3"><Link href={`/admin/users/${params.id}/edit`} className="rounded-md bg-slate-950 px-4 py-2 text-sm font-semibold text-white">Sua</Link><Link href="/admin/users" className="py-2 text-sm font-semibold">Quay lai</Link></div></div>{message ? <p className="text-sm text-rose-700">{message}</p> : user ? <div className="max-w-2xl rounded-md border border-slate-200 bg-white p-6"><dl className="grid gap-4 sm:grid-cols-2"><div><dt className="text-xs text-slate-500">Ho va ten</dt><dd className="font-semibold">{user.name}</dd></div><div><dt className="text-xs text-slate-500">Email</dt><dd>{user.email}</dd></div><div><dt className="text-xs text-slate-500">Vai tro</dt><dd>{user.role}</dd></div><div><dt className="text-xs text-slate-500">Trang thai</dt><dd>{user.status}</dd></div><div><dt className="text-xs text-slate-500">Don hang</dt><dd>{user.orders_count ?? 0}</dd></div></dl></div> : <p className="text-sm text-slate-500">Dang tai...</p>}</main>; }
