"use client";
import Link from "next/link"; import { useParams } from "next/navigation"; import { useEffect, useState } from "react"; import { apiGet } from "@/lib/api"; import type { Order } from "@/types/api";
export default function Page() { const params = useParams<{ id: string }>(); const [order, setOrder] = useState<Order | null>(null); const [message, setMessage] = useState(""); useEffect(() => { apiGet<Order>(`/admin/orders/${params.id}`).then(setOrder).catch((reason: Error) => setMessage(reason.message)); }, [params.id]); return <main className="mx-auto max-w-7xl px-4 py-8"><div className="mb-6 flex items-center justify-between"><h1 className="text-3xl font-bold text-slate-950">Chi tiet don hang</h1><Link href="/admin/orders" className="text-sm font-semibold">Quay lai</Link></div>{message ? <p className="text-sm text-rose-700">{message}</p> : order ? <div className="grid gap-6 lg:grid-cols-[1fr_1.4fr]"><section className="rounded-md border border-slate-200 bg-white p-6"><h2 className="text-xl font-bold">{order.code}</h2><dl className="mt-5 grid gap-4 sm:grid-cols-2"><div><dt className="text-xs text-slate-500">Khach hang</dt><dd>{order.customer_name}</dd></div><div><dt className="text-xs text-slate-500">Email</dt><dd>{order.customer_email}</dd></div><div><dt className="text-xs text-slate-500">Trang thai</dt><dd>{order.status}</dd></div><div><dt className="text-xs text-slate-500">Tong tien</dt><dd>{order.grand_total}</dd></div></dl></section><section className="rounded-md border border-slate-200 bg-white p-6"><h2 className="font-bold">San pham</h2><div className="mt-4 divide-y divide-slate-100">{order.items?.map((item) => <div key={item.id} className="py-3"><p className="font-semibold">{item.product_name}</p><p className="text-sm text-slate-500">{item.variant_name} · SL {item.quantity} · {item.subtotal}</p></div>)}</div></section></div> : <p className="text-sm text-slate-500">Dang tai...</p>}</main>; }
