"use client";
import Link from "next/link"; import { useParams } from "next/navigation"; import { useEffect, useState } from "react"; import { apiGet } from "@/lib/api"; import type { ReturnRequest } from "@/types/api";
export default function Page() { const params = useParams<{ id: string }>(); const [item, setItem] = useState<ReturnRequest | null>(null); const [message, setMessage] = useState(""); useEffect(() => { apiGet<ReturnRequest>(`/admin/returns/${params.id}`).then(setItem).catch((reason: Error) => setMessage(reason.message)); }, [params.id]); return <main className="mx-auto max-w-4xl px-4 py-8"><div className="mb-6 flex items-center justify-between"><h1 className="text-3xl font-bold text-slate-950">Chi tiet yeu cau doi tra</h1><Link href="/admin/returns" className="text-sm font-semibold">Quay lai</Link></div>{message ? <p className="text-sm text-rose-700">{message}</p> : item ? <section className="rounded-md border border-slate-200 bg-white p-6"><h2 className="text-xl font-bold">{item.code}</h2><dl className="mt-5 grid gap-4 sm:grid-cols-2"><div><dt className="text-xs text-slate-500">Ly do</dt><dd>{item.reason}</dd></div><div><dt className="text-xs text-slate-500">Trang thai</dt><dd>{item.status}</dd></div><div><dt className="text-xs text-slate-500">Hoan tien</dt><dd>{item.refund_status}</dd></div><div><dt className="text-xs text-slate-500">Ghi chu</dt><dd>{item.admin_note ?? "-"}</dd></div></dl><p className="mt-6 text-sm text-slate-600">{item.description ?? "Khong co mo ta."}</p></section> : <p className="text-sm text-slate-500">Dang tai...</p>}</main>; }
